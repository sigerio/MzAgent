# 按 todo 顺序执行后续修改

- 记录时间：2026-03-14
- 用户原始诉求：现在开始执行代码修改，按照todo项依次进行完成，并保证每项测试均通过
- 当前状态：执行中，已进入代码修改。

## 本轮执行目标
- 按既有详细任务清单顺序继续实施后续修改。
- 当前优先执行 `P1`：
  - 忽略策略补强
  - 测试映射表
  - live 使用说明补强
- 每完成一项先验证，再进入下一项。

## 当前已知约束
- 本轮属于项目问题，已获得用户明确同意，可进入代码修改。
- 不回退无关改动，不扩大本轮修改边界。
- 默认优先修改当前已完成测试收口对应的父仓/子仓文档与配置，不直接跳到新的功能扩展实现。

## 本轮执行进展（2026-03-14）
- `B1 忽略策略补强`：已完成。
  - 子仓已补 `.gitignore`，隔离 `.env`、`.mz_agent/`、`.venv/`、缓存目录与构建产物。
  - 已验证默认本地回归通过。
- `B2 测试映射表`：已完成。
  - 子仓 `README.md` 已补“测试域 -> 测试文件 -> 脚本入口”速查表。
  - 已验证默认本地回归通过。
- `B3 live 使用说明补强`：已完成。
  - 子仓 `README.md` 已补用途、前置条件、推荐顺序与失败归因说明。
  - 已验证默认本地回归通过。
- `B4 复核父仓 repos.md`：已完成复核，确认需要修改。
  - 原因：子仓 `origin` 实际已切换为 `git@github.com:sigerio/MzAgentSub.git`，但 `repos.md` 仍记录为 HTTPS。
  - 处理：本轮同步更新 `origin` 与 `source_checked_at`。
- 版本规则补充：
  - 本轮进入项目代码修改后，按规则补执行一次 patch 版本递增。
  - 子仓版本：`0.1.7 -> 0.1.8`

## 下一步
- `P1` 已全部完成并再次验证默认本地回归通过。
- `C1` 已完成：
  - 新增独立 `knowledge` 模块，补齐文档导入、分块、查询命中对象与 `KB -> RAG` 数据流。
  - 已通过 `./scripts/test_knowledge.sh` 与 `./scripts/test_all.sh`。
- `C2` 已完成：
  - `SkillAdapter` 已补目录扫描、frontmatter 元数据读取、资源清单暴露与 `SKL_001 ~ SKL_003` 错误码区分。
  - 已通过 `./scripts/test_skills.sh` 与 `./scripts/test_all.sh`。
- `C3` 已完成：
  - `perception` 已补 `normalized_goal`、`intent_summary`、`constraints`、`clarify_needed`。
  - 已通过 `./scripts/test_perception.sh` 与 `./scripts/test_all.sh`。
- `C4` 已完成：
  - `integration` 已从单轮 CLI 扩展到 `rag / skill / clarify` 三条跨层场景矩阵。
  - 已通过 `./scripts/test_integration.sh` 与 `./scripts/test_all.sh`。
- `C5` 已完成：
  - `README.md` 已补 live 联调结果记录规范。
- 当前默认本地回归结果：
  - 共 `69` 项通过。
  - `contracts` 19
  - `runtime` 11
  - `perception` 6
  - `stm` 2
  - `adapters` 9
  - `knowledge` 4
  - `skills` 5
  - `orchestration` 9
  - `integration` 4
- 下一步进入本轮收口：
  - 归档 active
  - 子仓提交并通过 SSH 推送
  - 父仓同步子模块指针与 worklog 后提交并通过 SSH 推送

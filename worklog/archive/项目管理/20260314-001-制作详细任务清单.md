# 制作详细任务清单

- 记录时间：2026-03-14
- 用户原始诉求：制作任务清单，列举详细的todo项
- 当前状态：已完成，已按 todo 顺序实施当前 P0 收口项。

## 本轮讨论目标
- 基于当前仓库真实状态整理一份详细任务清单。
- 区分“立即收口事项”“短期补强事项”“中期演进事项”。
- 输出可执行的 todo 列表，不执行代码修改。

## 当前已知约束
- 本轮属于项目问题，需先记录并进入讨论。
- 未获得用户明确同意前，不执行代码修改。
- 任务清单应基于当前仓库与 worklog 的真实状态，不按历史未落地设想直接视为待办。

## Team 分工（架构/实现/测试/风险）
- 架构：区分“当前实现面已完成事项”和“当前仍需推进事项”，避免清单失真。
- 实现：按父仓/子仓双仓结构拆分收口任务、文档任务和后续演进任务。
- 测试：把默认本地回归、live 联调和未来扩展测试区分开，不混成一个 todo。
- 风险：识别最容易导致误提交、误归档、误判完成的环节。

## 共识结论（是否通过 + 条件）
- 结论：通过，可以形成当前阶段的详细任务清单。
- 当前状态判断：
  - 子仓 `sub/MzAgentSub` 的“测试收口实现”已完成，但尚未提交推送。
  - 父仓的 `worklog`、索引和子模块指针联动收口尚未完成。
  - 默认本地回归已通过，live 联调仍属于独立补充项。
- 因此当前 todo 应分为三组：
  - 第一组：必须收口项
  - 第二组：建议补强项
  - 第三组：后续演进项

## 风险清单（高/中/低）
- 高：子仓尚未提交，若继续叠加改动，后续提交边界会越来越模糊。
- 高：父仓当前工作区本身较脏，若不先明确“本轮只收哪些内容”，容易把无关变更一并带入。
- 高：若不区分“默认本地回归”和“live 联调”，会误判外部依赖稳定性。
- 中：`worklog` 当前又新增了 active 问题，若后续要收口提交，需要先判断哪些该归档、哪些还要继续讨论。
- 中：`repos.md` 当前未反映这轮子仓版本从 `0.1.5 -> 0.1.6` 的变化；虽然本轮不是上游同步，但若要做父仓说明性收口，最好同步核对。
- 低：README 与脚本目前已对齐，但若后续继续加测试域，不同步就会再次漂移。

## 执行顺序（Phase 1..N）
- Phase 1：先做“本轮收口边界确认”，明确哪些文件属于本轮测试收口。
- Phase 2：先收子仓，再收父仓，不反过来。
- Phase 3：完成归档后，再按规则执行提交与推送。
- Phase 4：收口完成后，再讨论下一阶段功能扩展任务。

## 详细任务清单

### A. 必须收口项
- A1：复核子仓本轮变更边界
  - 核对本轮真正要收口的内容：
    - `pyproject.toml`
    - `README.md`
    - `scripts/test_*.sh`
    - `tests/` 下新增和迁移的测试文件
  - 识别不应进入提交的运行产物：
    - `.mz_agent/`
    - `.venv/`
    - 本地 `.env`
- A2：确认子仓版本变更说明
  - 核对子仓版本已从 `0.1.5 -> 0.1.6`
  - 为后续提交说明准备版本变更描述
- A3：核对默认本地回归结果
  - 保留当前已验证的回归结果：
    - `contracts` 19
    - `runtime` 11
    - `perception` 4
    - `stm` 2
    - `adapters` 9
    - `knowledge` 3
    - `skills` 2
    - `orchestration` 9
    - `integration` 1
  - 确认后续提交说明中要写“默认回归 60 项通过”
- A4：归档当前已完成的测试规划问题
  - 复核本轮新 active 中哪些只是讨论确认，哪些需要继续保留
  - 若进入正式收口，至少需要处理：
    - `20260314-003-测试收口后所有todo项是否具备测试项与方法`
    - `20260314-001-制作详细任务清单`
- A5：执行子仓收口
  - 在 `sub/MzAgentSub` 内完成：
    - 状态核对
    - `git add`
    - 中文 commit
    - `git push origin`
- A6：执行父仓联动收口
  - 更新父仓中的：
    - 子模块指针
    - `worklog/index.md`
    - `worklog/history.md`
    - 本轮新增/归档的 `worklog` 文档
  - 然后在父仓执行：
    - 状态核对
    - 中文 commit
    - `git push origin`

### B. 建议补强项
- B1：清理并规范本地运行产物策略
  - 明确 `.env`、`.mz_agent/`、`.venv/` 是否应忽略
  - 如果当前仓库缺少忽略策略，后续需要补 `.gitignore` 或等价方案
- B2：补一份“测试域 -> 测试文件 -> 脚本入口”映射表
  - 便于后续快速审查和回归
  - 也能降低 README 再次漂移的概率
- B3：补充 live 测试使用门槛说明
  - 明确 `test_live_llm.sh`
  - 明确 `test_live_mcp.sh`
  - 明确它们不属于默认阻断门禁
- B4：复核父仓 `repos.md`
  - 虽然本轮不是上游同步，但建议确认当前记录是否仍符合“当前有效仓库映射”的表述
  - 若你希望版本信息也体现当前本地推进，可单独讨论是否要更新

### C. 后续演进项
- C1：把当前 `knowledge` 测试从最小 `RAGAdapter` 行为，扩展到真正的知识库载体实现
- C2：把当前 `skills` 测试从适配壳，扩展到真实目录扫描、元数据读取和资源暴露链
- C3：把当前 `perception` 测试从输入准备逻辑，扩展到更明确的意图提取、约束抽取和澄清判定
- C4：把当前 `integration` 从单轮 CLI 闭环，扩展到更完整的跨层场景矩阵
- C5：为 live 联调补单独的结果记录规范
  - 哪些环境变量必须存在
  - 哪些失败可归因于外部环境
  - 哪些失败属于代码问题

## 推荐优先级
- P0：
  - 子仓收口提交推送
  - 父仓联动收口提交推送
  - worklog 归档补齐
- P1：
  - 运行产物忽略策略
  - 测试映射表
  - live 使用说明补强
- P2：
  - knowledge / skills / perception / integration 的真实能力扩展

## 执行记录（2026-03-14）
- 已进入实施阶段，执行原则为：
  - 完成一项
  - 验证通过
  - 再进入下一项
- 当前正在处理：
  - 子仓本地运行产物隔离
  - 子仓版本号按规则执行 patch `+1`

## 本轮执行结果（2026-03-14）
- 已完成子仓运行产物隔离：
  - 新增 `.gitignore`
  - 隔离 `.env`、`.mz_agent/`、`.venv/`、`__pycache__`、`.pytest_cache/`、`src/mz_agent.egg-info/`
- 已按版本规则将子仓版本从 `0.1.6` 提升到 `0.1.7`
- 已重新验证默认本地回归：
  - `./scripts/test_all.sh` 通过
  - 共 60 项通过
- 已归档本轮判定类问题：
  - `20260314-003-测试收口后所有todo项是否具备测试项与方法`
- 已完成子仓收口：
  - 提交：`e4ac4da6a`
  - 已通过 SSH 推送到 `origin/main`
- 当前剩余事项：
  - 父仓同步子模块指针
  - 父仓同步 `worklog/index.md` 与 `worklog/history.md`
  - 父仓提交并推送

# MzAgents 技术路径规划

- 记录时间：2026-03-02
- 用户原始诉求：src/目录下提供了两种 agents 框架，想基于此在 code/ 目录下开发自己的 MzAgents，先规划技术路径。
- 当前状态：讨论中（未获执行同意，不进行代码修改）。

## 现状核对
- `src/agentscope/`：包含完整框架源码、示例与文档。
- `src/HelloAgents/`：已补齐完整框架代码与文档（可用于架构对照）。
- `code/`：当前仅有 `.env`，尚未建立项目骨架。

## 用户澄清（2026-03-02）
- 目标是基于两种框架的设计思路完成 `MzAgents` 核心软件。
- 不把这两种框架作为运行时依赖进行接入。
- 继续遵循约束：不生成总结性 Markdown 文档、不生成测试脚本、不编译、不运行。

## 新增输入（2026-03-02）
- 用户确认：`src/HelloAgents/` 内容已补充完成。
- 现状更新：`HelloAgents` 已包含 `core/agents/tools/context/observability` 等完整子系统，可用于架构对照与能力拆分。



## 已确认选择（2026-03-02）
- 首期对外接口：CLI 优先。

## 共识方向（已修正）
- 采用“参考实现驱动”的自研路线：
  - 参考源 A：`AgentScope`（接口抽象、消息流、扩展点）
  - 参考源 B：`HelloAgents`（已补齐，用于能力对照）
  - 目标产物：`code/` 下独立运行的 `MzAgents` 核心，不依赖上述框架包
- 首期聚焦“核心会话内核 + 统一模型网关 + 最小工具执行面”。

## 待执行阶段（草案）
- Phase 1：领域模型与协议定义（消息、会话、工具、错误）
- Phase 2：核心引擎实现（调度循环、上下文窗口、状态管理）
- Phase 3：模型适配层实现（OpenAI 兼容接口优先）
- Phase 4：工具系统实现（注册/调用/权限边界）
- Phase 5：对话 API 暴露（CLI 或 HTTP 二选一先落地）


## 讨论状态（2026-03-02）
- 用户通过 `cunzhi` 选择：先保持讨论，不进入实现执行。


## 用户新确认（2026-03-02）
- 实现核心路径：`CLI 方式 + 自建 Core 代码`。
- 下一步诉求：进入 `MzAgents` 核心能力讨论与分层定义。


## 用户方向纠偏（2026-03-02）
- 用户要求先讨论智能体的笼统功能，不先做技术细分。
- 在笼统功能确认后，再映射到 MzAgents 的底层 Core 功能分层。


## 新讨论点（2026-03-02）
- 用户提出能力设想：
  - 根据用户输入自动提取相关领域。
  - 按领域在“本地数据库 + 云端数据”中进行分类检索。
  - 自动选择不同提示词模板，重编排用户输入后再与 LLM 交互。
- 用户问题：该思路是否可行。


## 新讨论点（2026-03-02-2）
- 用户希望继续讨论该链路涉及的智能体能力清单与边界。


## 新讨论点（2026-03-02-3）
- 用户提出判断：当前设想更像 Skill 而不是 Agent。
- 讨论目标：明确 MzAgents 中 Agent 与 Skill 的职责边界与组合关系。

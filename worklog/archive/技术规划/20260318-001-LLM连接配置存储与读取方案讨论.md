# LLM 连接配置存储与读取方案讨论

- 记录时间：2026-03-18
- 用户原始诉求：发起新的讨论，讨论LLM连接配置的存储方案和读取方案
- 当前状态：进入方案讨论，未执行代码修改。

## 背景现状

- 当前仓库已经采用 `profile` 配置方案模型。
- 当前配置仓位于 `sub/MzAgentSub/.mz_agent/llm_profiles.json`。
- 当前运行时装载路径为：`.env` 兜底生成默认方案 → `llm_profiles.json` 覆盖/持久化 → `load_runtime_settings(profile_name=...)` 解析当前实际配置。
- 当前密钥未进入 `STM`，这是正确方向。

## 本轮讨论目标

1. 评估当前 LLM 配置的持久化位置是否合理。
2. 评估当前读取链路是否足够清晰、稳定、可维护。
3. 明确后续应保留的原则、需要调整的点，以及推荐收敛方向。

## 讨论焦点

- `profile` 配置是否继续独立于 `.env` 和 `STM`
- `.env` 是否还应该承担默认方案生成职责
- 运行时每次按 `profile_name` 重载配置是否合理
- 配置仓、默认方案、当前活跃方案三者之间的职责边界是否清楚
